# TaxiTips — Status Report

Generated by an inspection pass over the actual repo contents (`taxitips-api`, `taxitips-app`, `taxitips-web`), the live migration files, and the Flutter client. Nothing here is assumed from the briefing — everything below was verified by reading code.

Note on naming: the briefing refers to `/api`, `/app`, `/web`. The actual repo uses `taxitips-api`, `taxitips-app`, `taxitips-web`. This doc uses the real names.

---

## 0. Update 2026-09-08 — Spår B: the app now reads from Django

Verified by running it, not by reading it: the Flutter app can now take its
tips from `taxitips-backend` instead of the Supabase RPCs, and the numbers
match the RPC row-for-row on live data (same id set, same ordering inputs).

**What moved.** `get_smart_alerts`, `get_opportunity_detail` and
`current_entitlement` have Python ports in
[core/api.py](taxitips-backend/core/api.py),
[core/thresholds.py](taxitips-backend/core/thresholds.py) and
[core/entitlement.py](taxitips-backend/core/entitlement.py). The SQL
functions still exist and still work — the app falls back to them when
`API_BASE_URL` is unset or the backend doesn't answer — but the judgement
is made in one place now instead of three (Python score, plpgsql selection,
Dart thresholds).

**Three things the move exposed, all now fixed:**

1. **Feedback has never been recorded.** `alert_feedback.alert_id` is a
   correctly-typed `uuid` FK — but it references `alerts(id)`, while the app
   has been sending *opportunity* ids since the `opportunities` split. The
   two id spaces overlap in 0 of 4345 rows, so every 👍/👎 was rejected by
   the database and swallowed by the client's `catch`. `alert_feedback` has
   zero rows. This corrects §3.4 above: the type mismatch it describes is
   not what's wrong; the target table is. New `opportunity_feedback` table
   (Django-owned) points at `opportunities`.
2. **Coordinate-less tips only reached three regions.** The RPC had three
   hardcoded lat/lon boxes (Skåne, Stockholm, Göteborg), so a driver in
   Falun or Umeå never saw a tip without coordinates regardless of
   `MARKET_SCOPE=national`. `thresholds.market_region()` derives the market
   from `REGION_ANCHOR` — all fifteen regions.
3. **Compensation never reached a driver.** `compensation_eligible` /
   `compensation_amount_kr` were computed by `core/compensation.py` but the
   RPC predates the fields and never selected them. Now on the card and in
   the detail sheet.

**Threshold duplication** (the `50` in schema/constants.md) is down from four
copies to three: `api_client.dart`'s is gone, `pipeline_view.py` reads the
constant. `severity_labels.dart` keeps one deliberately, as the fallback for
cached rows with no `level`. Remaining: the Node worker (being retired) and
viz `server.js`, which can read `/api/config`.

**Same day, second pass — the sixth source and the visible pipeline:**

* **Road was never ported to Django.** Both `taxi_relevance.py` and
  `text_scoring.py` carried a branch raising
  `NotImplementedError("no road source in Django")`, so the Django pipeline
  had five of the six sources. `core/sources/trafikverket_road.py` is the
  port (176 events for Skåne alone; 462 across the three market counties).
  Road stays capped at 15 points and now travels in its own `context` field
  rather than the driver's list — measured 129 road rows against 5 transit
  tips in Skåne, which would have buried the only thing worth driving to.
* **A dead source looked exactly like a quiet day.** Trafiklab's key answers
  `429 has exceeded its quota` on all fourteen regions, and nothing in the
  system said so — `source_events` simply had no rows for it, which is what a
  calm traffic day looks like too. `SourceStatus` (core/health.py) now records
  every poll's outcome, and the pipeline view shows the source red with its
  error.
* **Next Departure was prose, not data.** The rail pipeline has computed
  "next departure in N minutes" and "last departure today" since Fas 2, but
  they only survived as a sentence inside `reasons` — nothing could filter,
  sort or calibrate on them. Now columns on `opportunities`, with coverage
  (21 of 293 active tips) shown as its own section, since that ratio *is* the
  measure of how far Transport Gap (P1) has got.
* **`rail_station` had zero rows** while every poll cycle re-fetched the same
  ~1000 stations into process memory. Now persisted (718 rows).

**Third pass — API-inventering, ersättningsregler och "vad gör resenären i stället?":**

* **Ersättningsreglerna är researchade per län och seedade.** 16 regioner med
  tröskel, tak, undantagna färdsätt och frist, varje rad med ordagrant citat
  och källänk (`docs/transit-compensation-rules.md`). Tre rättelser mot
  tidigare underlag: EU 1371/2007 är upphävd (ersatt av 2021/782 den
  2023-06-07), och under den nya förordningen ersätts **taxi inte alls** för
  fjärrtåg. Prisbasbeloppet 2026 (59 200 kr) verifierat mot regeringen.se;
  Skånetrafikens avvikande 1/20-regel verifierad mot deras egen sida.
  `cap_per_person` är trelägad — fem huvudmän skriver ut "per resenär", två
  "per resa", nio säger inget, och då säger appen inget heller.
* **API-inventering av alla sex källor** (`docs/api-field-inventory.md`, 784
  rader): fält för fält, med mätt ifyllnadsgrad och 19 konkreta förslag.
  Tio är genomförda. De två som ändrade mest verklighet: väghändelser använde
  `PublicationTime` som starttid (medianfel 862 timmar — ett vägarbete från
  2024 såg nyskapat ut varje cykel), och `MessageType` kallar en helt avstängd
  väg "Vägarbete" medan `MessageCode` säger "Vägen avstängd" (25 av 33
  felklassade som `low`).
* **Nästa avgång och ersättningstrafik når föraren.** Signalerna fanns i
  pipelinen men stannade i poängformeln. Nu: absolut avgångstid (minuter
  åldras, klockslag gör det inte), ersättningstrafik via `Deviation.Code`
  före textmatchning, bussavgångar i tågflödet utpekade som bussar, och
  Stockholm har för första gången ett svar på "när går nästa?" via SL:s
  departures-endpoint.
* **Ett färskt hissfel kunde bli ett tips.** `FACILITY/LIFT` föll tidigare
  bort på åldersfiltret, alltså av tur; verifierat att "Avstängd hiss vid
  Skanstull" nådde flödet innan filtret skrevs.

**Still open from the P0 list:** service-role key, verified backups, push
send pipeline, duplicate Stripe handlers.

---

## 1. Confirmed current behavior

**Backend**: self-hosted Supabase (Postgres/Auth/PostgREST) on Coolify, service `supabase-taxitips`. Schema covers profiles, companies, company_members (roles: company_owner/company_admin/driver), billing_accounts, subscriptions, devices (driver units, joined via company `join_code`), device_transfer_codes, tickets, app_config, and `alerts` (the disturbance/opportunity table).

**Worker** (`taxitips-api/worker`): a single long-running Node/Express process that:
- Polls Trafiklab (GTFS-RT service alerts, Skåne + neighboring regions) and Trafikverket (road situations) on a fixed interval (`POLL_INTERVAL_MS`, default 60s).
- Scores every alert with a hand-tuned regex/keyword ruleset ([taxiRelevance.js](taxitips-api/worker/src/taxiRelevance.js)) — Swedish-language pattern matching for "serious" vs "medium" vs "ignore", hub-name detection, Skåne-geofencing.
- Computes an H3 index (`h3-js`, resolution 8) and a `demand_score` (0–100) via `calculateDemandSignal`, including a "Last Train Risk" boost for late-night train disruptions.
- Upserts directly into `public.alerts` — raw source data and derived score live in the **same row, same table**.
- Also hosts the Stripe webhook endpoint (`POST /webhooks/stripe`) with idempotency via `processed_webhook_events`.

**Client read path**: Flutter app calls `get_smart_alerts(lat, lon)`, a `SECURITY DEFINER` RPC that computes Haversine distance and a `worth_it_score` (`demand_score - distance_km*2`, zeroed if time-to-expiry is tighter than distance+10 min) directly in SQL, then returns JSON.

**Billing**: company-level Stripe subscription. Signup flow ([signup, in api_client.dart](taxitips-app/lib/api_client.dart:240)) creates a company + `company_owner` membership, then immediately calls `createCheckoutSession`. Stripe webhook updates `companies.status` and upserts into `subscriptions` on any `customer.subscription.*` event.

**Push**: FCM token registration exists client-side ([push_service.dart](taxitips-app/lib/push_service.dart)) and tokens are stored on `devices.push_token`. That's it — see gap below.

**Web** (`taxitips-web`): as of 2026-09, this **is** marketing-only again. The user decided to stop managing companies/billing/admin from the web — `admin.html`/`admin.js`, `dashboard.html`/`dashboard.js`, and `portal.css` were removed. All company/account/billing management is intended to live in the Flutter app's admin-mode; a separate superadmin panel for the user personally is planned later but not built yet. `login.html` is now a static "open the app" page (no Supabase login form), and `signup.html`'s success state points to the app instead of a web dashboard or Stripe checkout redirect. `verify.html`/`reset.html`/`claim.html` remain as Supabase Auth/device-claim redirect targets. `app.html` (the driver PWA/Leaflet map) is unaffected — it's product, not admin.

---

## 2. Stubs / unfinished features

| Item | Evidence |
|---|---|
| **`entitlements()` is a literal stub** | [api_client.dart:374](taxitips-app/lib/api_client.dart:374) — `Future<Map<String, dynamic>> entitlements() async => {'ok': true};`. Never called anywhere in the app (`grep` for `entitlements` across screens returns nothing). No UI reads company `status` to gate anything. |
| **No server-side entitlement check on data access** | `get_smart_alerts` RPC has no auth/entitlement logic at all — see Security section. |
| **No push dispatch** | Nothing in the worker or an edge function sends an FCM push when a high-value alert appears. The pipeline registers tokens but never uses them. The entire "reliable push: dedup, expiry, source-health monitoring" P0 item is not partially done — it's absent. |
| **Duplicate, inconsistent Stripe webhook handlers** | The real handler is in the worker ([index.js](taxitips-api/worker/src/index.js):21-72), which updates `companies`/`subscriptions`. A second handler exists as a Deno edge function ([stripe-webhook/index.ts](taxitips-api/supabase/functions/stripe-webhook/index.ts)) that verifies the signature but **only marks the event processed — it never calls any equivalent of `handleStripeEvent`**. If Stripe is pointed at the edge function URL instead of (or as well as) the worker, subscription state silently never updates. Needs to be resolved to one canonical handler. |
| **`addMember` is unimplemented** | [api_client.dart:828](taxitips-app/lib/api_client.dart:828) throws `501` and tells the admin to use Supabase Studio directly. |
| **`ALL_MIGRATIONS.sql` is stale** | It does not include `20260829000001_smart_signals.sql` or `20260829000002_rpc_worth_it.sql` (0 matches for `alert_feedback`/`get_smart_alerts`). Per [README.md](taxitips-api/README.md), this file is the documented path for applying schema via Studio's SQL Editor for non-CLI environments. If production was bootstrapped this way, `get_smart_alerts`, `demand_score`, `h3_index`, and `alert_feedback` may not exist there — which would explain the defensive `PGRST205`/"alerts table missing" fallback already coded into [api_client.dart:717-733](taxitips-app/lib/api_client.dart:717). |

---

## 3. Security gaps (P0-relevant)

1. **`alerts` table is publicly readable with no gate.** RLS policy: `alerts_select_authenticated ... for select using (true)` ([rls.sql:70-71](taxitips-api/supabase/migrations/20260823000002_rls.sql:70)) — the comment even says "public read for driver apps using anon + RPC" as if this were an intentional MVP shortcut, not the final state. Since `alerts` now carries `demand_score`, `h3_index`, and `reasons` (added in the smart_signals migration), this table **is** the premium "opportunities" data the briefing explicitly says must never be public.
2. **`get_smart_alerts` RPC has zero entitlement/auth check.** It's `SECURITY DEFINER`, callable by anyone with the anon key (which is compiled into every client), and returns full scored results with no company/subscription check inside the function body at all ([rpc_worth_it.sql](taxitips-api/supabase/migrations/20260829000002_rpc_worth_it.sql)).
3. **No `entitlements` concept exists in Postgres at all** — no function, no view, nothing. The briefing's required model (`entitlements()` as canonical server-side truth) has to be built from scratch, not fixed.
4. **`alert_feedback.alert_id` is `uuid` but `alerts.id` is `text`.** This FK is a type mismatch — it cannot have been successfully applied against a database where `alerts` already existed with `text` ids, or it was applied against an empty/different environment. Either way, `submitAlertFeedback` in the Flutter client ([api_client.dart:641](taxitips-app/lib/api_client.dart:641)) passes a `text`-shaped id and will fail against a correctly-typed column, or the constraint was never actually enforced. Needs to be fixed as part of the P0 schema pass, not deferred.
5. **DB credentials**: `.env.example` shows the worker using `SUPABASE_SERVICE_ROLE_KEY` (full bypass-RLS access) for all of its writes, including the join/scoring pipeline. The briefing requires a least-privilege role split between migrations and runtime writes — currently there is only ever one role in use (service_role) for the worker's entire write path.
6. **No automated backup evidence found** — no backup scripts, no S3/Coolify backup config in the repo. Should be treated as not yet implemented, matching the briefing's P0 item.

---

## 4. Billing sync state

- Idempotency is handled correctly at the webhook layer via `processed_webhook_events` (insert-then-check pattern, [index.js:45-54](taxitips-api/worker/src/index.js:45)).
- Only events prefixed `customer.subscription.` are handled. `checkout.session.completed` (needed to capture `stripe_customer_id` on first purchase if not already linked) and `invoice.payment_failed` (needed for dunning/grace period — note `subscriptions.grace_until` exists in the schema but nothing appears to set it) are not handled.
- Sync failures are logged to `processed_webhook_events.error` but nothing reads that column back out — no alerting, no retry job. This half-satisfies the briefing's "no silent sync failures" rule (visible in DB) but doesn't fulfill "retryable."
- The duplicate edge-function handler (see above) is the single biggest billing risk right now — it's a live foot-gun if Stripe's webhook URL ever points there.

---

## 5. Data-ingestion state

- Trafiklab + Trafikverket pollers both have working mock-mode fallbacks (`TRAFIKLAB_API_KEY=mock`, `TRAFIKVERKET_MOCK=1`), which is good for local dev.
- Scoring logic (`taxiRelevance.js`) is a substantial, carefully-tuned Swedish regex ruleset — this is real product logic, not a stub, but it's centralized in one file with hardcoded thresholds (not the "centrally configured" externalized rule model the briefing asks for; "centrally configured" here just means "one file," not database-driven or hot-reloadable).
- **`source_events` vs `opportunities` separation does not exist.** Raw polled data and the derived score are merged into one `alerts` row at ingestion time ([poller.js:mapAlert](taxitips-api/worker/src/poller.js:20)). This is the core P1 architectural item and is a genuine rework, not a rename.
- SMHI is not integrated at all yet (correctly deferred to P2 per the briefing).
- Confidence/explainability: `reasons text[]` exists and is populated, so the "which source_events, which rule, what confidence" requirement is partially met for the *rule name*, but there's no link from an `alerts` row back to which raw poll/API response produced it (no raw-event table, no foreign key) — can't reconstruct provenance after the fact.

---

## 6. Push pipeline state

Registration-only. Token capture, storage, and refresh work. There is no code path anywhere in the worker, an edge function, or a cron job that reads newly-scored high-value alerts and calls FCM's send API. This is 100% greenfield work, not a bug fix.

---

## 7. Proposed order of fixes (against the P0/P1 list)

> **STALE per 2026-09-09.** Allt nedanför är skrivet mot den gamla
> Supabase/Node-arkitekturen: `alerts`-tabellen, RPC:n `get_smart_alerts`
> och edge functions. Pipelinen bor sedan dess i Django
> (`taxitips-backend/`, grenen `fas2-django-pipeline`), och punkt 1–4 och 7
> är lösta eller obsoleta där. **Läs `AGENTS.md` §8 för det verifierade
> läget** — den tabellen är kollad mot koden, inte mot minnet. Behåller
> listan här för historiken och för P1-punkterna 9–13, som fortfarande
> gäller.

**P0 — do these first, in this order:**
1. Fix the `alerts.id` / `alert_feedback.alert_id` type mismatch (blocks everything else touching that table cleanly).
2. Split `alerts` conceptually enough to add a real entitlement gate *without* waiting for the full `source_events`/`opportunities` rework — i.e., lock down RLS + rewrite `get_smart_alerts` to check company entitlement before returning scored rows. This is the single highest-leverage fix: it closes the "premium data is public" hole immediately.
3. Build `entitlements()` as a real Postgres function (company status + subscription state → boolean/tier), wire it into `get_smart_alerts`, and have the Flutter client actually call and respect it (currently calls nothing).
4. Resolve the duplicate Stripe webhook handlers down to one canonical path; add `checkout.session.completed` and `invoice.payment_failed` handling.
5. Create a scoped least-privilege Postgres role for the worker's runtime writes, separate from whatever role runs migrations.
6. Stand up automated Postgres backups (pg_dump → S3-compatible storage on Coolify) with a verified test restore.
7. Build the actual push-send path (worker or new edge function reads newly high-scored alerts → FCM), with dedup and expiry.
8. Regenerate `ALL_MIGRATIONS.sql` so the documented Studio-paste path isn't silently stale, or deprecate that doc path in favor of the CLI-only flow if that's the actual deployment method now.

**P1 — after P0 lands:**
9. Do the real `source_events` → `opportunities` split (this is a schema migration + worker rewrite, expand/migrate/contract per the briefing's rule).
10. GTFS static context + Next Departure.
11. Last Departure Risk + Transport Gap (some of this logic already exists ad hoc inside `calculateDemandSignal`'s "Last Train Risk" — formalize it as its own rule under the new model).
12. Stranded Score v1, Personal Worth-It v1 (partially exists as `worth_it_score` in the RPC — needs to move from ad-hoc SQL expression to the centrally-configured rule engine), driver action cards.
13. ~~🚕/👍/👎 feedback capture~~ — done via `opportunity_feedback` (see §0). What's left is the UI for 🚕 "kör dit": the client method takes a `verdict`, but only 👍/👎 are wired to buttons.

**P2**: as specified in the briefing, no changes.

---

## 8. Repo/doc discrepancies worth flagging

- ~~Briefing assumes `/web` is "minimal, marketing only"~~ — resolved 2026-09: the admin/dashboard portal (`admin.html`, `dashboard.html`, `portal.css`, `admin.js`, `dashboard.js`) was removed by user decision; `taxitips-web` is marketing/funnel-only again, matching the briefing's original scoping. Company/billing management is intended to live in `taxitips-app`'s admin-mode instead — worth confirming that mode actually covers what the removed web portal used to do (seat/plan changes, Stripe portal link, company member management) before calling this migration complete.
- `taxitips-app/lib/api_client_http_legacy.dart` exists alongside the current Supabase-based `api_client.dart` — dead code candidate, not investigated further; flag for cleanup once confirmed unused.
- `.playwright-mcp/` logs are committed under `taxitips-app/` (untracked currently, per git status) — likely scratch output from an earlier session, not project source.
