.DS_Store
.env
**/.env
**/node_modules/
taxitips-app/.dart_tool/
taxitips-app/build/
taxitips-app/.flutter-plugins-dependencies
*.iml
.idea/
.agents/
taxitips-app/.playwright-mcp/
taxitips-api/supabase/.branches/
taxitips-api/supabase/.temp/

# Leadlistor med namn, adress, telefon, e-post och styrelsenamn (3 677 poster).
# Personuppgifter hör inte hemma i git-historiken -- den går inte att gallra.
# Filerna ligger kvar lokalt och ingår inte i något byggkontext.
ops/crm/

# Felsökningslogg från Firebase CLI. Brus, inte källkod.
firebase-debug.log

# Android upload-signing — aldrig i git.
taxitips-app/android/key.properties
taxitips-app/android/keystore/

# Lokal venv för Play Store-uppladdning.
taxitips-app/.playstore-venv/
mcp-server.log
